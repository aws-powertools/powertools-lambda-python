schema {
    query: Query
}

type Query {
    getTodo(id: ID!): Todo  # will match a `def get_todo(id: str)` function
    listTodos: [Todo]       # will match a `def list_todos()` function
}

type Todo {
    id: ID!
    title: String
    description: String
    done: Boolean
}
