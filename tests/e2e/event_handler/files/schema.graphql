schema {
    query: Query
}

type Query {
    getPost(id:ID!): Post
    allPosts: [Post]
}

type Post {
    id: ID!
    author: String!
    title: String
    content: String
    url: String
    ups: Int
    downs: Int
    relatedPosts: [Post]
}